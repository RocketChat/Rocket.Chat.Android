package chat.rocket.android.contacts.adapter

import android.view.View
import chat.rocket.android.chatrooms.adapter.ViewHolder

class PermissionsViewHolder(itemView: View) : ViewHolder<PermissionsItemHolder>(itemView) {
    override fun bindViews(data: PermissionsItemHolder) {
    }
}
