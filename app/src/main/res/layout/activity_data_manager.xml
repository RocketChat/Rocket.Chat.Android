<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/relativeLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/AppTheme"
    tools:context=".datamanager.ui.DataManagerActivity">

    <include
        android:id="@+id/layout_app_bar"
        layout="@layout/app_bar_data_manager" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/data_usage_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="20dp"
        android:layout_below="@+id/layout_app_bar">

        <TextView
            android:id="@+id/data_usage_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintHorizontal_bias=".05"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:text="Total: 1.8 Gb\n\nSent: 0.6 Gb\nReceived: 1.2 Gb"
            android:textSize="20sp"
            android:textStyle="1"/>

        <ImageView
            android:id="@+id/data_usage_chart"
            android:layout_width="100dp"
            android:layout_height="100dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toRightOf="@+id/data_usage_text"
            app:layout_constraintRight_toRightOf="parent"
            app:srcCompat="@drawable/circle_green" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical"
        android:layout_below="@+id/data_usage_layout"
        tools:listitem="@layout/item_data_manager_chat"/>

</RelativeLayout>