<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="25dp"
    android:orientation="vertical">

    <TextView
        android:id="@+id/text_enter_name"
        android:text="Enter a name for your custom theme:"
        android:textColor="?colorPrimaryText"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content" />

    <EditText
        android:id="@+id/edit_text_theme_name"
        style="@style/Authentication.EditText.Border"
        android:layout_width="match_parent"
        android:layout_marginTop="16dp"
        android:hint="Theme Name"
        android:imeOptions="actionDone"
        android:inputType="text" />

    <TextView
        android:id="@+id/text_select_base_theme"
        android:text="Select your base theme:"
        android:textColor="?colorPrimaryText"
        android:paddingTop="16dp"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content" />

    <ListView
        android:id="@+id/lv_items"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="@null"
        android:paddingTop="8dp" />
</LinearLayout>